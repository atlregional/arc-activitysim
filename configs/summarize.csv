Description,Output,Expression
# Top summary

# Count households
,households_count,persons_merged[['household_id']].nunique().rename('households')

# Count tours
,tours_count,tours_merged.reset_index()[['tour_id']].count().rename('tours')

# Count trips
,trips_count,trips_merged.reset_index()[['trip_id']].count().rename('trips')

# Count persons
,persons_count,persons_merged[['household_id']].count().rename('persons')

# Count person-tours
,person_tours,"trips_merged[['tour_id', 'number_of_participants']].drop_duplicates()[['number_of_participants']].sum().rename('tours')"

# Count person-trips
,person_trips,trips_merged[['number_of_participants']].sum().rename('trips')

# Count tours per household
,_tours,"trips_merged[['tour_id', 'number_of_participants']].drop_duplicates()['number_of_participants'].sum()"
,_households,persons_merged['household_id'].nunique()
,tours_per_household_count,"pd.Series(_tours / _households, name='tours_per_household')"

# Count trips per household
,_trips,trips_merged['number_of_participants'].sum()
,trips_per_household_count,"pd.Series(_trips / _households, name='trips_per_household')"

# Work location
,_work_location,persons_merged[persons_merged['distance_to_work'].notna()]
,_work_location_without_wfh,"_work_location[(_work_location['distance_to_work']!=0) & (_work_location['ptype'].isin([1,2]))]"
,_work_distance, _work_location_without_wfh['distance_to_work']
,_counts_of_work_distance_bin,"spaced_intervals(_work_distance, 0, 1, '{right:,.0f}').value_counts()"
,work_location,"_counts_of_work_distance_bin.sort_index().rename('trips').reset_index()"

# School location
,_school_location,persons_merged[persons_merged['distance_to_school'].notna()]
,_school_location_without_sfh,"_school_location[(_school_location['distance_to_school']!=0) & (_school_location['ptype'].isin([3,6,7,8]))]"
,_school_distance, _school_location_without_sfh['distance_to_school']
,_counts_of_school_distance_bin,"spaced_intervals(_school_distance, 0, 1, '{right:,.0f}').value_counts()"
,school_location,"_counts_of_school_distance_bin.sort_index().rename('trips').reset_index()"

# CDAP distribution
,cdap_distribution,persons_merged.groupby('cdap_activity_category').size().reset_index().set_index('cdap_activity_category').T

# CDAP by person type
,_cdap_by_person_type,"persons_merged.groupby(['person_type', 'cdap_activity_category']).size().rename('persons').reset_index()"
,cdap_by_person_type,"_cdap_by_person_type.pivot_table(index='person_type', columns='cdap_activity_category', values='persons', fill_value=0).reset_index()"

# Value of time
# ,_vot,persons_merged['value_of_time']
# ,_counts_of_vot_bin,"spaced_intervals(_vot, 0, 1, '{right:,.0f}').value_counts()"
# ,value_of_time,"_counts_of_vot_bin.sort_index().rename('persons').reset_index()"

# Work from home
# ,_work_from_home,"persons_merged[persons_merged['ptype'].isin([1,2])]"
# ,work_from_home,"_work_from_home.groupby('work_from_home').size().reset_index().set_index('work_from_home').T"

# Telecommuting
# ,_telecommuting,"persons_merged[persons_merged['ptype'].isin([1,2])]"
# ,_telecommuting_without_wfh,"_telecommuting[_telecommuting['work_from_home']==False]"
# ,telecommuting,"_telecommuting_without_wfh.groupby('telecommute_frequency').size().reset_index().set_index('telecommute_frequency').T"

# Auto ownership
,auto_ownership,households.groupby('auto_ownership_category').size().rename('households').reset_index()

# Auto ownership by household income
,_auto_ownership_by_hh_income,"households.groupby(['auto_ownership_category', 'hh_income_category']).size().rename('households').reset_index()"
,auto_ownership_by_hh_income,"_auto_ownership_by_hh_income.pivot_table(index='hh_income_category', columns='auto_ownership_category', values='households', fill_value=0).reset_index()"

# Auto ownership by household size
,_auto_ownership_by_hh_size,"households.groupby(['auto_ownership_category', 'household_size']).size().rename('households').reset_index()"
,auto_ownership_by_hh_size,"_auto_ownership_by_hh_size.pivot_table(index='household_size', columns='auto_ownership_category', values='households', fill_value=0).reset_index()"

# Auto ownership by number of drivers
,_auto_ownership_by_number_of_drivers,"households.groupby(['auto_ownership_category', 'number_drivers']).size().rename('households').reset_index()"
,auto_ownership_by_number_of_drivers,"_auto_ownership_by_number_of_drivers.pivot_table(index='number_drivers', columns='auto_ownership_category', values='households', fill_value=0).reset_index()"

# Auto ownership by number of workers
,_auto_ownership_by_number_of_workers,"households.groupby(['auto_ownership_category', 'number_workers']).size().rename('households').reset_index()"
,auto_ownership_by_number_of_workers,"_auto_ownership_by_number_of_workers.pivot_table(index='number_workers', columns='auto_ownership_category', values='households', fill_value=0).reset_index()"

# Work tours length
,_work_tour_distance,"trips_merged[trips_merged['tour_type'] == 'work'][['tour_id','distance_tour']].drop_duplicates()"
,_counts_of_work_tour_distance_bin,"spaced_intervals(_work_tour_distance['distance_tour'], 0, 1, '{right:,.0f}').value_counts()"
,work_tour_length,"_counts_of_work_tour_distance_bin.sort_index().rename('tours').reset_index()"

# School tours length
,_school_tour_distance,"trips_merged[trips_merged['tour_type'] == 'school'][['tour_id','distance_tour']].drop_duplicates()"
,_counts_of_school_tour_distance_bin,"spaced_intervals(_school_tour_distance['distance_tour'], 0, 1, '{right:,.0f}').value_counts()"
,school_tour_length,"_counts_of_school_tour_distance_bin.sort_index().rename('tours').reset_index()"

# Non-mandatory tours length
,_non_mandatory_tour_distance,"trips_merged[trips_merged['tour_category'] == 'non_mandatory'][['tour_id','distance_tour']].drop_duplicates()"
,_counts_of_non_mandatory_tour_distance_bin,"spaced_intervals(_non_mandatory_tour_distance['distance_tour'], 0, 1, '{right:,.0f}').value_counts()"
,non_mandatory_tour_length,"_counts_of_non_mandatory_tour_distance_bin.sort_index().rename('tours').reset_index()"

# Work tours start/end time
,_work_tours,tours_merged[tours_merged['tour_type'] == 'work']
,work_tours_start_time,_work_tours.groupby('start').size().sort_index().rename('tours').reset_index()
,work_tours_end_time,_work_tours.groupby('end').size().sort_index().rename('tours').reset_index()

# School tours start/end time
,_school_tours,tours_merged[tours_merged['tour_type'] == 'school']
,school_tours_start_time,_school_tours.groupby('start').size().sort_index().rename('tours').reset_index()
,school_tours_end_time,_school_tours.groupby('end').size().sort_index().rename('tours').reset_index()

# Non-mandatory tours start/end start time
,_non_mandatory_tours,tours_merged[tours_merged.tour_category == 'non_mandatory']
,non_mandatory_tours_start_time,_non_mandatory_tours.groupby('start').size().sort_index().rename('tours').reset_index()
,non_mandatory_tours_end_time,_non_mandatory_tours.groupby('end').size().sort_index().rename('tours').reset_index()

# Joint tour by composition
,_joint_tours,tours_merged[tours_merged.tour_category == 'joint']
,joint_tours_by_composition,"_joint_tours.groupby('composition').size().reset_index().set_index('composition').T"

# Trip mode share
,trips_by_mode_count,trips_merged.groupby('trip_mode')[['number_of_participants']].sum().T

# Count trips by purpose
,trips_by_purpose_count,trips_merged.groupby('primary_purpose_trip')[['number_of_participants']].sum().T

# Count trips by purpose and departure time
,trip_purpose_by_time_of_day,"trips_merged.groupby(['depart','primary_purpose_trip'])['number_of_participants'].sum().unstack(-1).reset_index()"

# Count trips with each combination of tour mode and trip mode (for Sankey)
,tour_mode_to_trip_mode,"trips_merged.groupby(['tour_mode','trip_mode']).size().rename('trips').reset_index()"

